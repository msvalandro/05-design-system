{
  "name": "@ignite-ui/design-system",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "packageManager": "npm@9.6.7",
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build"
  },
  "devDependencies": {
    "turbo": "^2.0.6"
  }
}
